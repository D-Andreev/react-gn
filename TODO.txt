- Init command
    Prerequisites:
    - Initialize Config
    - Implement Cra (Communication with CRA)
        - createApp()
    - Implement scanDirectory
        - constructor(storage)
        - getFileStructure(path)
    - Implement Cli.askQuestion
    - Make Cra.createApp accept flags
    - Make Cra.ejectApp

    Work:
    - Check target dir
        - If it's not empty ask for confirmation
    - Generate base app with cra
    - if (withCra is true)
        - Ejected app
    - Get file structure with scanDirectory
    - Apply all options from config and set in file structure
    - Modify files based on file structure

