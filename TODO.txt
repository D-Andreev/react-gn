- Init command
    Prerequisites:
    - Initialize Config
    - Implement Cra (Communication with CRA)
        - createApp()
    - AppReader
        - constructor(storage)
        - getFileStructure(path)

    Work:
    - if (withCra is true)
        - Generate base app with cra
    - if (withCra is true)
        - Generate ejected app

    - Read file structure from files
    - Apply all options from config and set in file structure
    - Generate files based on file structure
